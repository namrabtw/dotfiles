-- Import the required module
local ibl = require('ibl')

-- Set up the configuration
ibl.setup({
	indent = {
		char = "▏",
	},
	scope = {
		char = "▏",
		show_start = false,
		show_end = false
	}
})

